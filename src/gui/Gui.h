#pragma once
class Gui
{
public:
	void Setup();
private:
	// we generally want some sort of object of a selection (misc, combat, etc)
	// create the node for that and then display with the features
};